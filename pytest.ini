[pytest]
# Add the repo root to sys.path so `import database` works from tests/ on all platforms.
# Without this, pytest on Ubuntu CI determines rootdir as tests/ and the root
# modules (database.py, ticket_generator.py, ai_triage.py) are not importable.
pythonpath = .
testpaths = tests
