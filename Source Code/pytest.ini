[pytest]
# Add src/ to sys.path so `import database` resolves to src/database.py from tests/.
# Matches PYTHONPATH=/home/user/app/src set in Dockerfile for runtime consistency.
pythonpath = src
testpaths = tests
